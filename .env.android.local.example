# Copiá este archivo a .env.android.local si usás celular físico (misma Wi‑Fi que la PC).
# Reemplazá la IP por la de tu máquina (ipconfig → IPv4).
VITE_API_URL=http://192.168.1.100:5055/api
VITE_DEV_API_PROXY=http://localhost:5055
VITE_ENABLE_ADMIN=true
VITE_APP_SCHEME=udlclub
